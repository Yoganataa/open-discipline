export interface Change { filePath:string; addedText:string; source:"write"|"edit"|"apply_patch"; }
interface PatchFile { filePath:string; operation:"add"|"update"|"delete"; movePath?:string; addedLines:string[]; }
export function extractChanges(toolName:string,args:Record<string,unknown>):Change[]{if(toolName==="write"){const f=typeof args.filePath==="string"?args.filePath:undefined,c=typeof args.content==="string"?args.content:undefined;return f&&c!==undefined?[{filePath:f,addedText:c,source:"write"}]:[];}if(toolName==="edit"){const f=typeof args.filePath==="string"?args.filePath:undefined,c=typeof args.newString==="string"?args.newString:typeof args.content==="string"?args.content:undefined;return f&&c!==undefined?[{filePath:f,addedText:c,source:"edit"}]:[];}if(toolName==="apply_patch"){const p=typeof args.patchText==="string"?args.patchText:undefined;return p?parseApplyPatch(p):[];}return[];}
export function parseApplyPatch(text:string):Change[]{const lines=text.replace(/\r\n/g,"\n").split("\n"),files:PatchFile[]=[];let cur:PatchFile|undefined;const flush=()=>{if(cur)files.push(cur);cur=undefined;};for(const line of lines){const m=/^\*\*\* (Add|Update|Delete) File:\s*(.+?)\s*$/.exec(line);if(m){flush();cur={filePath:m[2]!,operation:m[1]!.toLowerCase() as PatchFile["operation"],addedLines:[]};continue;}const mv=/^\*\*\* Move to:\s*(.+?)\s*$/.exec(line);if(mv&&cur){cur.movePath=mv[1];continue;}if(cur&&line.startsWith("+")&&!line.startsWith("+++"))cur.addedLines.push(line.slice(1));}flush();const out:Change[]=[];for(const f of files){if(f.operation==="delete")continue;const t=f.addedLines.join("\n");out.push({filePath:f.filePath,addedText:t,source:"apply_patch"});if(f.movePath)out.push({filePath:f.movePath,addedText:t,source:"apply_patch"});}return out;}
